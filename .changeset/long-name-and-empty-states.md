---
'kmitl-course-planner': patch
---

A long Thai course name no longer overflows its catalog card and a long plan name no longer overflows the poster: the card and the shelf truncate with the full name on a hover, and the poster wraps the name so nothing is hidden in an export. The empty grid hint and the import error listing each gained an icon so they read as designed states.
