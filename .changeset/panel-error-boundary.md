---
'kmitl-course-planner': patch
---

A render error in the panel now shows a recovery card with a reload action instead of white screening the host page. The boundary wraps the panel body, so the header keeps its close control and the host registration page behind the overlay is untouched.
